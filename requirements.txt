# ══════════════════════════════════════════════
# QuantumTrade Dependencies
# ══════════════════════════════════════════════

# ─── Core Data ───
pandas>=2.0.0
numpy>=1.24.0

# ─── Market Data ───
yfinance>=0.2.28
ccxt>=4.0.0

# ─── Brokers ───
alpaca-py>=0.21.0          # Alpaca (US stocks + crypto)
python-binance>=1.0.19     # Binance (crypto)

# ─── Machine Learning ───
scikit-learn>=1.3.0
mlflow>=2.9.0          # MLflow for model registry & tracking
scipy>=1.11.0          # Statistical tests (KS test)

# ─── MLOps / Model Serving ───
fastapi>=0.109.0        # REST API for model serving
uvicorn[standard]>=0.27.0  # ASGI server
aiohttp>=3.9.0          # Async HTTP client for alerts

# ─── Technical Analysis ───
ta>=0.11.0

# ─── Web / Dashboard ───
flask>=3.0.0
flask-cors>=4.0.0

# ─── Desktop GUI ───
PyQt5>=5.15.0

# ─── Configuration ───
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
pyyaml>=6.0.0  # YAML config support

# ─── Notifications ───
requests>=2.31.0
python-telegram-bot>=21.0

# ─── Scheduling ───
schedule>=1.2.0

# ─── WebSocket Streaming ───
websocket-client>=1.6.0

# ─── Database ───
# psycopg2-binary>=2.9.0  # PostgreSQL driver (optional, install if needed)
sqlalchemy>=2.0.0       # ORM (optional)

# ─── Redis ───
redis>=5.0.0  # Redis Python client with Streams support

# ─── Logging ───
colorama>=0.4.6
structlog>=23.0.0  # Structured logging (optional)
prometheus-client>=0.17.0
psutil>=5.9.0

# ─── OpenTelemetry (Distributed Tracing) ───
opentelemetry-api>=1.20.0
opentelemetry-sdk>=1.20.0
opentelemetry-exporter-jaeger-thrift>=1.20.0
opentelemetry-instrumentation-logging>=1.20.0
opentelemetry-instrumentation-requests>=1.20.0
opentelemetry-instrumentation-redis>=1.20.0
opentelemetry-instrumentation-sqlalchemy>=1.20.0
opentelemetry-instrumentation-fastapi>=1.20.0  # Future API
PyJWT>=2.9.0  # JWT auth (HS256)

# ─── Testing ───
pytest>=7.4.0
pytest-cov>=4.1.0

# ─── Development ───
mypy>=1.5.0          # Type checking
black>=23.0.0        # Code formatting
ruff>=0.1.0          # Linting
